server:
  port: 0
spring:
  application:
    name: customer-service
eureka:
  client:
    serviceUrl:
       defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
welcome:
  label: updated Welcome....example...Now Chill !!
